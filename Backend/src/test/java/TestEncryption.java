import org.junit.Assert;
import org.junit.Test;

import java.io.FileNotFoundException;

public class TestEncryption {

//    @Test
//    public void testEncryptUrl() throws FileNotFoundException {
//        String encryptedUrl = encryption.encryptText(url);
//        System.out.println(encryptedUrl);
//        Assert.assertNotEquals("Encrypted url and the actual url should not equal", encryptedUrl, url);
//    }
//
//    @Test
//    public void testDecryptUrl() {
//        String encryptedUrl = encryption.encryptText(url);
//        System.out.println(encryption.decryptText(encryptedUrl));
//        Assert.assertEquals("Decrypted url and the actual url should equal", encryption.decryptText(encryptedUrl), url);
//    }
}
